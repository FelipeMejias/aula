// Inserir e retirar elementos de uma lista
let lista = [1, 2, 3];
lista.push(4);  // Adiciona no final
console.log(lista);  // Saída: [1, 2, 3, 4]

lista.pop();  // Remove o último elemento
console.log(lista);  // Saída: [1, 2, 3]
