// Usando break, continue e pass
for (let i = 0; i < 5; i++) {
    if (i === 3) {
        break;  // Interrompe o loop quando i é igual a 3
    }
    if (i === 2) {
        continue;  // Pula a iteração quando i é igual a 2
    }
    console.log(i);
}
